<div id="<%= dom_id category %>">
  <p>
    <%= category.name %>
  </p>
  <%= link_to t('common.actions.edit'), edit_category_path(category)%>
  <%= link_to t('common.actions.delete'), category_path(category), data: { turbo_method: :delete, turbo_confirm: t('common_confirm') }%>
</div>
